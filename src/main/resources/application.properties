spring.application.name=BancodeDados
# --- Banco de Dados ---
spring.datasource.url=jdbc:mariadb://localhost:3306/controle-abate-uf-municipio
spring.datasource.username=root
spring.datasource.password=549276183
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# --- Configurações de servidor (opcional) ---
server.port=8085

# --- CSV (opcional) ---
# Local onde o CSV será lido
csv.file.path=src/main/resources/abate.csv
